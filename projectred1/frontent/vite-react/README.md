# Vite + React demo

Quick scaffolded Vite + React app. To run locally:

```bash
cd frontent/vite-react
npm install
npm run dev
```

This folder is intentionally minimal — I can migrate your existing pages into React components next.
